# Optional: enables AI-powered goal parsing and explanations
# Without this key, the app uses rule-based fallbacks (fully functional)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Set to false to force rule-based mode (useful when API quota is exceeded)
USE_LLM=true
